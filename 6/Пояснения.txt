* Продумайте - какие части функционала можно вынести в базовый (родительский) класс TextFile, а какие - сделать в унаследованном от него классе GuestBook

Я думаю в данном случе в класс TextFile можно вынести все свойства и методы, которые написаны, так как при работе с текстовыми данными - нужно получать данные, пополнять и сохранять ... Но я для примера вынес только метод получения всех данных getData() поскольку возможность прочитать файл нужна всегда, а записывать может все-таки в каком-то случае и не нужна ...

***************
* Попробуйте некоторые методы заканчивать конструкцией return $this; и придумайте этому применение
Возвращается текущий объект.
Возможно иногда необходимо для того, чтобы, например, мы меняем защищенные свойства объекта, а затем возвращаем его, чтобы можно было дальше с этим объектом работать .. хотя нет ... если мы создали объект, присвоили имя и вызвали метод, который меняет защищенные свойства, то это объект и так будет изменен, не нужно его возвращать для этого ...

Может, вот: Например, мы приняли данные с формы и у нас происходит проверка данных ... Если проверка прошла успешно, то возвращается объект, например, Пользователь... А если не прошла проверка - возвращается null